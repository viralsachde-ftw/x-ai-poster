# ── X session cookies (for posting) ──────────────────────────────────
# Get from browser: F12 > Application > Cookies > https://x.com
# Copy auth_token and ct0 values. Last ~12 months.
X_AUTH_TOKEN=
X_CT0=

# ── xAI / Grok API key (for post generation) ────────────────────────
# Get from https://console.x.ai/
XAI_API_KEY=

# ── Cron secret ──────────────────────────────────────────────────────
# Any random string to protect cron endpoints from unauthorized calls
CRON_SECRET=

# ── Vercel KV / Upstash Redis (REST API) ─────────────────────────────
# Required for: daily auto-posts, duplicate detection, post history
# Get from Vercel Dashboard > Storage > KV, or Upstash console
# NOTE: REDIS_URL won't work — you need the REST API url + token
# Accepts any of these pairs:
#   KV_REST_API_URL + KV_REST_API_TOKEN (vercel default)
#   KV_REST_URL + KV_REST_TOKEN
#   UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN (upstash default)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# ── Residential proxy (optional) ─────────────────────────────────────
# Only needed if you get AUTOMATION_DETECTED errors
# Format: http://username:password@host:port
PROXY_URL=
